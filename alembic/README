Generic single-database configuration.

## Criar migration:

- ``` alembic revision --autogenerate -m "Migração inicial!" ```

## Aplicar migration

- ``` alembic upgrade head ```

## Limpar o registro do alembic no banco

- para se conectar com bd ```psql -h <host> -U <username> -d <database>```

- para achar ```SELECT * FROM alembic_version;```

- para deletar ```DELETE FROM alembic_version;```